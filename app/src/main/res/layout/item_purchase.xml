<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="item"
            type="com.katsiro.alexey.gia.data.entities.Purchase" />
    </data>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <TextView
            android:id="@+id/text_id"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:text="@{``+item.id}"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
            tools:text="0" />

        <TextView
            android:id="@+id/text_сategory_id"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:text="@{String.valueOf(item.categoryId)}"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
            tools:text="1" />

        <TextView
            android:id="@+id/text_note"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:text="@{item.note}"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
            tools:text="Макарон" />
        <TextView
            android:id="@+id/text_date"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:text="@{item.formattedDate}"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
            tools:text="24:02" />
        <TextView
            android:id="@+id/text_cost"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:text="@{String.valueOf(item.cost)}"
            android:textAppearance="@style/TextAppearance.MaterialComponents.Body2"
            tools:text="10.2 р" />
    </LinearLayout>


</layout>